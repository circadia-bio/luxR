# dev/

Real-data smoke tests and diagnostic scripts, excluded from the formal
`testthat` suite. Participant-derived data files placed here should never
be committed — add specific filenames/patterns to `.gitignore` as needed.
